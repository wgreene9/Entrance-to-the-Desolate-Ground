country_decisions = {
	EDG_DAN_dissolution_of_the_danish_federation = { #解散丹麦邦联
		major = yes
		potential = {
			has_global_flag = EDG_DAN_enable_states_of_danish_federation_flag
			OR = {
				controls = 12
				owns = 12
				any_neighbor_country = {
					has_country_flag = EDG_DAN_member_states_of_danish_federation_flag
				}
			}
			NOT = {
				has_country_flag = EDG_DAN_member_states_of_danish_federation_flag
			}
		}
		allow = {
			OR = {
				controls = 12
				owns = 12
			}
		}
		effect = {
			custom_tooltip = EDG_DAN_dissolution_of_the_danish_federation.tt
			add_prestige_or_monarch_power = { amount = 50 }
			hidden_effect = {
				clr_global_flag = EDG_DAN_enable_states_of_danish_federation_flag
				every_country = {
					limit = {
						has_country_flag = EDG_DAN_member_states_of_danish_federation_flag
					}
					clr_country_flag = EDG_DAN_member_states_of_danish_federation_flag
					break_alliance = SLL
					break_alliance = LLL
					break_alliance = FYN
					break_alliance = ABO
					break_alliance = ABG
					break_alliance = KLG
					break_alliance = SYS
				}
				every_country = {
					limit = {
						has_country_flag = EDG_DAN_presidency_states_of_danish_federation_flag
					}
					clr_country_flag = EDG_DAN_presidency_states_of_danish_federation_flag
				}
			}
		}
		ai_will_do = {
			factor = 1
		}
	}
}