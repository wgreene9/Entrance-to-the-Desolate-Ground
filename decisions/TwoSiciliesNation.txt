country_decisions = {
	two_sicilies_nation = {
		major = yes
		potential = {
			normal_or_historical_nations = yes
			was_never_end_game_tag_trigger = yes
			NOT = { has_country_flag = formed_two_sicilies_flag }
			OR = {
				ai = no
				is_playing_custom_nation = no
			}
			OR = {
				is_free_or_tributary_trigger = yes
				ai = no
			}
			NOT = { exists = TTS }
			NOT = { tag = TTS }
			NOT = { tag = ITA }
			NOT = { tag = IT1 }
			OR = {
				primary_culture = neapolitan
				primary_culture = sicilian
			}
			is_colonial_nation = no
			OR = {
				is_former_colonial_nation = no
				AND = {
					is_former_colonial_nation = yes
					ai = no
				}
			}
		}
		provinces_to_highlight = {
			OR = {
				OR = {
					province_id = 121
					province_id = 125
				}
				OR = {
					AND = {
						OR = {
							culture = sicilian
							culture = neapolitan
						}
						ROOT = {
							NOT = {
								num_of_owned_provinces_with = {
									value = 10
									OR = {
										culture = sicilian
										culture = neapolitan
									}
									region = italy_region
									owned_by = ROOT
								}
							}
							owns_core_province = 121
							owns_core_province = 125
						}
					}
				}
			}
			OR = {
				NOT = { owned_by = ROOT }
				NOT = { is_core = ROOT }
			}
		}
		allow = {
			if = {
				limit = {
					has_country_flag = has_EDG_TTS_mission_3_4_flag
				}
				custom_tooltip = EDG_can_Ignoring_technology_form_two_sicilies_nation_tt
			}
			else = {
				adm_tech = 10
			}
			is_free_or_tributary_trigger = yes
			is_nomad = no
			is_at_war = no
			owns_core_province = 121
			owns_core_province = 125
			num_of_owned_provinces_with = {
				OR = {
					culture = sicilian
					culture = neapolitan
				}
				region = italy_region
				value = 10
			}
		}
		effect = {
			change_tag = TTS
			if = {
				limit = {
					NOT = { tag = NAP }
					NOT = { was_tag = NAP }
				}
				swap_non_generic_missions = yes
			}
			remove_non_electors_emperors_from_empire_effect = yes
			if = {
				limit = {
					NOT = {
						government_rank = 2
					}
				}
				set_government_rank = 2
			}
			set_country_flag = formed_two_sicilies_flag
			add_prestige_or_monarch_power = { amount = 10 }
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = SIC_ideas }
				}
				country_event = { id = ideagroups.1 } #Swap Ideas
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}


	
	EDG_IT1_nation = {#成立意大利帝国
		major = yes
		potential = {
			normal_or_historical_nations = yes
			was_never_end_game_tag_trigger = yes
			NOT = { has_country_flag = formed_IT1_flag }
			OR = {
				ai = no
				is_playing_custom_nation = no
			}
			OR = {
				is_free_or_tributary_trigger = yes
				ai = no
			}
			NOT = { exists = ITA }
			NOT = { exists = IT1 }
			NOT = { tag = IT1 }
			NOT = { tag = ITA }
			NOT = { tag = PAP }
			NOT = { tag = HLR }
			OR = {
				primary_culture = neapolitan
				primary_culture = sicilian
			}
			OR = {
				ai = no
				AND = {
					ai = yes
					num_of_cities = 15
				}
			}
			is_colonial_nation = no
			OR = {
				is_former_colonial_nation = no
				AND = {
					is_former_colonial_nation = yes
					ai = no
				}
			}
		}
		provinces_to_highlight = {
			OR = {
				AND = {
					region = italy_region
					root = {
						NOT = {
							num_of_owned_provinces_with = {
								value = 20
								region = italy_region
								is_core = root
							}
						}
						#owns_core_province = 101	
						owns_core_province = 104	
						owns_core_province = 116	 
						owns_core_province = 121	
						owns_core_province = 125	
					}
				}
				#province_id = 101	# Genoa
				province_id = 104	# Milan
				province_id = 116	# Firenze 
				province_id = 121 	# Napoli
				province_id = 125	# Palermo
			}
			OR = {
				NOT = { owned_by = ROOT }
				NOT = { is_core = ROOT }
			}
		}
		allow = {
			if = {
				limit = {
					has_country_flag = EDG_TTS_mission_3_11_flag
				}
				custom_tooltip = EDG_TTS_mission_3_11_tt_decision
			}
			else = {
				adm_tech = 10
			}
			is_free_or_tributary_trigger = yes
			is_at_war = no
			#owns_core_province = 101	# Genoa
			owns_core_province = 104	# Milan
			owns_core_province = 116	# Firenze 
			owns_core_province = 121 	# Napoli
			owns_core_province = 125	# Palermo
			num_of_owned_provinces_with = {
				value = 30
				region = italy_region
				is_core = root
			}
		}
		effect = {
			country_event = { id = EDG_flavor_TTS.25 }
			if = {
				limit = {
					is_part_of_hre = yes
					is_elector = no
					is_emperor = no
				}
				emperor = {
					add_opinion = {
						who = root
						modifier = opinion_left_empire
					}
				}
				set_in_empire = no
				emperor = {
					country_event = { id = hre_event.5 }
				}
			}
			if = {
				limit = {
					NOT = { government_rank = 3 }
				}
				hidden_effect = {
					set_in_empire = no
				}
				set_government_rank = 3
			}
			change_tag = IT1
			#this is so that a tier 1 reform is always selected, preventing the user from just never selecting one and enjoying loads of absolutism
			
			italy_region = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_permanent_claim = ROOT
			}
			if = {
				limit = { has_custom_ideas = no }
				country_event = { id = ideagroups.1 } #Swap Ideas
			} 
			swap_non_generic_missions = yes
			set_country_flag = formed_IT1_flag
		}
		ai_will_do = {
			factor = 400
			modifier = {
				factor = 0
				NOT = {
					calc_true_if = {
						#owns_core_province = 101	# Genoa
						owns_core_province = 104	# Milan
						owns_core_province = 116	# Firenze 
						owns_core_province = 121 	# Napoli
						owns_core_province = 125	# Palermo
						amount = 2
					}
				}
			}
			modifier = {
				factor = 0
				NOT = {
					num_of_cities = 10
				}
			}
			modifier = {
				factor = 0
				NOT = { adm_tech = 8 }
			}
		}
	}

	
}