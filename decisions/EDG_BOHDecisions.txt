country_decisions = {
	EDG_BOH_survey_of_keli = {
		potential = {
			has_country_flag = EDG_BOH_start_survey_flag
			has_disaster = EDG_BOH_inversion_of_tarot
		}
		major = yes
		allow = {
			if = {
				limit = { 
					NOT = {
						has_country_flag = EDG_BOH_has_clue_flag
					}
				}
				custom_trigger_tooltip = {
					tooltip = EDG_BOH_survey_of_keli.trigger.tt
					has_country_flag = EDG_BOH_has_clue_flag
				}
			}
			else = {
				custom_trigger_tooltip = {
					tooltip = EDG_BOH_survey_of_keli.trigger_clue.tt
					has_country_flag = EDG_BOH_has_clue_flag
				}
			}
			treasury = 250
			
		}
		effect = {
			add_treasury = -250
			custom_tooltip = EDG_SPACE_TT
			if = {
				limit = { 
					check_variable = {
						which = EDG_BOH_clue_var
						value = 21
					}
				}
				country_event = {
					id = EDG_BOHDisasterEvents.102
					days = 3
				}
			}
			else_if = {
				limit = { 
					check_variable = {
						which = EDG_BOH_clue_var
						value = 20
					}
				}
			}
			hidden_effect = {
				clr_country_flag = EDG_BOH_has_clue_flag
			}
		}
	}
}