namespace = EDG_DANEvents

country_event = {
	id = EDG_DANEvents.0
	is_triggered_only = yes
	title = EDG_DANEvents.0.name
	desc = EDG_DANEvents.0.desc
	picture = BAD_WITH_MONARCH_eventPicture

	immediate = {
		hidden_effect = {
			set_country_flag = EDG_DAN_member_states_of_danish_federation_flag
			add_country_modifier = {
				name = EDG_IRE_long_cherished_wish
				duration = -1
			}
			if = {
				limit = { 
					NOT = {
						tag = LLL
					}
				}
				create_alliance = LLL
				add_historical_friend = LLL
			}
			if = {
				limit = { 
					NOT = {
						tag = FYN
					}
				}
				create_alliance = FYN
				add_historical_friend = FYN
			}
			if = {
				limit = { 
					NOT = {
						tag = ABO
					}
				}
				create_alliance = ABO
				add_historical_friend = ABO
			}
			if = {
				limit = { 
					NOT = {
						tag = ABG
					}
				}
				create_alliance = ABG
				add_historical_friend = ABG
			}
			if = {
				limit = { 
					NOT = {
						tag = KLG
					}
				}
				create_alliance = KLG
				add_historical_friend = KLG
			}
			if = {
				limit = { 
					NOT = {
						tag = SYS
					}
				}
				create_alliance = SYS
				add_historical_friend = SYS
			}
			add_historical_friend = SLL
			every_rival_country = {
				root = {
					remove_rival = prev
				}
			}
		}
	}

	option = {
		name = EDG_DANEvents.0.aa
		custom_tooltip = EDG_DANEvents.0.aa.tt
		add_country_modifier = {
			name = EDG_DAN_danish_confederation
			duration = -1
		}
	}
}
country_event = {
	id = EDG_DANEvents.1
	is_triggered_only = yes
	title = EDG_DANEvents.1.name
	desc = EDG_DANEvents.1.desc
	picture = REFORM_eventPicture

	immediate = {
		hidden_effect = {
			set_global_flag = EDG_DAN_enable_states_of_danish_federation_flag
			set_country_flag = EDG_DAN_member_states_of_danish_federation_flag
			set_country_flag = EDG_DAN_presidency_states_of_danish_federation_flag
			if = {
				limit = { 
					NOT = {
						tag = LLL
					}
				}
				create_alliance = LLL
				add_historical_friend = LLL
			}
			if = {
				limit = { 
					NOT = {
						tag = FYN
					}
				}
				create_alliance = FYN
				add_historical_friend = FYN
			}
			if = {
				limit = { 
					NOT = {
						tag = ABO
					}
				}
				create_alliance = ABO
				add_historical_friend = ABO
			}
			if = {
				limit = { 
					NOT = {
						tag = ABG
					}
				}
				create_alliance = ABG
				add_historical_friend = ABG
			}
			if = {
				limit = { 
					NOT = {
						tag = KLG
					}
				}
				create_alliance = KLG
				add_historical_friend = KLG
			}
			if = {
				limit = { 
					NOT = {
						tag = SYS
					}
				}
				create_alliance = SYS
				add_historical_friend = SYS
			}
			every_rival_country = {
				root = {
					remove_rival = prev
				}
			}
		}
	}

	option = {
		name = EDG_DANEvents.1.aa
		custom_tooltip = EDG_DANEvents.1.aa.tt
		add_country_modifier = {
			name = EDG_DAN_danish_confederation
			duration = -1
		}
		add_prestige = 100
	}
}