namespace = EDG_HSAACtionEvents

country_event = {
	id = EDG_HSAACtionEvents.0
	is_triggered_only = yes
	hidden = yes

	trigger = {
		has_country_flag = EDG_HSA_mission_2_6_flag
	}

	immediate = {
		every_province = {
			limit = {
				has_active_triggered_province_modifier = EDG_HSA_trading_post_province_triggered_modifier
			}
			root = {
				add_treasury = 48
			}
		}
	}

	option = {
	}
}