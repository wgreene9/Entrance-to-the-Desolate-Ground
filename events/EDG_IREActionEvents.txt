namespace = EDG_IREActionEvents

country_event = {
	id = EDG_IREActionEvents.0
	is_triggered_only = yes
	hidden = yes

	trigger = {
		has_estate_privilege = estate_EDG_bard_spreading_culture
	}

	immediate = {
		if = {
			limit = { 
				is_city = yes
				any_owned_province = {
					is_core = root
				}
			}
			random_owned_province = {
				limit = {
					NOT = {
						culture_group = gaelic
					}
				}
				change_culture = root
			}
		}
		country_event = {
			id = EDG_IREActionEvents.0
			days = 1825
		}
	}

	option = {
		
	}
}