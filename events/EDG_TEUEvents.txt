namespace = EDG_TEUEvents

country_event = {
	id = EDG_TEUEvents.0
	is_triggered_only = yes
	title = EDG_TEUEvents.0.name
	desc = EDG_TEUEvents.0.desc
	picture = CRUSADER_HORDE_eventPicture

	option = {
		name = EDG_TEUEvents.0.aa
		41 = {
			move_capital_effect = yes
		}
		change_tag = PRU
		on_change_tag_effect = yes
		restore_country_name_effect = yes
		swap_non_generic_missions = yes
		add_government_reform = EDG_crusader_kingdom_of_prussia_reform
		east_prussia_area = {
			limit = {
				NOT = { owned_by = ROOT }
			}
			add_permanent_claim = PRU
		}
		west_prussia_area = {
			limit = {
				NOT = { owned_by = ROOT }
			}
			add_permanent_claim = PRU
		}
		add_prestige = 25
		every_neighbor_country = {
			limit = { government = monarchy }
			add_opinion = { who = ROOT modifier = secularized_monastic_order }
		}
		add_country_modifier = {
			name = "centralization_modifier"
			duration = 3650
		}
		set_government_rank = 2
		set_country_flag = formed_prussia_flag
		if = {
			limit = {
				has_custom_ideas = no
				NOT = { has_idea_group = PRU_ideas }
			}
			country_event = { id = ideagroups.1 } #Swap Ideas
		}
		hidden_effect = {
			if = {
				limit = {
					has_dlc = "Lions of the North"
					has_country_flag = teu_prussian_path
				}
				unlock_government_reform = {
					government_reform = prussian_monarchy_base
				}
				unlock_government_reform = {
					government_reform = prussian_republic_reform
				}
				unlock_government_reform = {
					government_reform = prussian_theocratic_reform
				}
			}
		}
	}
	# option = {
	# 	name = EDG_TEUEvents.0.bb
	# 	add_government_reform = prussian_monarchy
	# }
}
country_event = {
	id = EDG_TEUEvents.1
	is_triggered_only = yes
	title = EDG_TEUEvents.1.name
	desc = EDG_TEUEvents.1.desc
	picture = DIPLOMACY_eventPicture
	major = yes
	major_trigger = {
		is_part_of_hre = yes
	}

	option = {
		name = EDG_TEUEvents.1.aa
		from = {
			add_ruler_modifier = {
				name = EDG_TEU_eastern_garrison_modifier
				duration = -1
			}
		}
		custom_tooltip = EDG_TEUEvents.1.aa.tt
		add_imperial_influence = 10
		ai_chance = {
			factor = 1000
		}
	}
	option = {
		name = EDG_TEUEvents.1.bb
		from = {
			set_ruler_flag = EDG_TEU_refuse_eastern_garrison_flag
		}
		add_imperial_influence = -5
		ai_chance = {
			factor = 0
		}
	}
}

country_event = {
	id = EDG_TEUEvents.500
	is_triggered_only = yes
	hidden = yes

	immediate = {
		set_emperor_of_china = root
	}

	option = {
	}
}