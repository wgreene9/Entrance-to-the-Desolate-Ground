namespace = EDG_GOTEvents

country_event = {
	id = EDG_GOTEvents.0
	is_triggered_only = yes
	title = EDG_GOTEvents.0.name
	desc = EDG_GOTEvents.0.desc
	picture = EDG_GOT_eventPicture

	option = {
		name = EDG_GOTEvents.0.aa
		country_event = {
			id = EDG_GOTEvents.1
		}
	}
}

country_event = {
	id = EDG_GOTEvents.1
	is_triggered_only = yes
	title = EDG_GOTEvents.1.name
	desc = EDG_GOTEvents.1.desc
	picture = EDG_GOT_eventPicture

	option = {
		name = EDG_GOTEvents.1.aa
		country_event = {
			id = EDG_GOTEvents.2
		}
	}
}
country_event = {
	id = EDG_GOTEvents.2
	is_triggered_only = yes
	title = EDG_GOTEvents.2.name
	desc = EDG_GOTEvents.2.desc
	picture = EDG_GOT_eventPicture

	option = {
		name = EDG_GOTEvents.2.aa
		if = {
			limit = {
				NOT = { government = republic }
			}
			change_government = republic
			add_republican_tradition = 100
		}
		else = {
			add_stability_or_adm_power = yes
		}
		hidden_effect = {
			set_country_flag = got_pirate_missions
		}
		add_government_reform = pirate_republic_reform
		update_sprite_pack = yes
		EDG_GOT_define_klaus_stotbeck = yes
		swap_non_generic_missions = yes
		if = {
			limit = {
				any_owned_province = {
					has_port = yes
				}
			}
			random_owned_province = {
				limit = {
					has_port = yes
				}
				galley = root
				galley = root
				galley = root
				light_ship = root
				light_ship = root
				light_ship = root
				EDG_GOT_define_flagship_jackdaw = yes
			}
		}
		add_treasury = 1000
		hidden_effect = {
			enable_estate_effect = {
				estate = estate_EDG_voyager
			}
			country_event = {
				id = FOICommonEvents.4
				days = 3
			}
		}
		ai_chance = {
			factor = 9
		}
	}
	option = {
		name = EDG_GOTEvents.2.bb
		ai_chance = {
			factor = 1
		}
	}
}

country_event = {
	id = EDG_GOTEvents.500
	is_triggered_only = yes
	hidden = yes

	immediate = {
		change_tag = DDF
		swap_free_idea_group = yes
		add_government_reform = EDG_GOT_scare_fleet_reform
	}

	option = {
		
	}
}
country_event = {
	id = EDG_GOTEvents.501
	is_triggered_only = yes
	hidden = yes

	immediate = {
		add_government_power = {
			mechanic_type = EDG_GOT_infarmy_mechanic
			power_type = EDG_GOT_infarmy
			value = 50
		}
	}

	option = {
		
	}
}