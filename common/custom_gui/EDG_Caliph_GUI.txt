
custom_button = {#已关闭
	name = Caliph_GUI_off_button
	tooltip = Caliph_GUI_off_button_tt
	potential = {
		has_country_flag = EDG_Caliph_GUI_off_flag
		religion_group = muslim
	}
	trigger = {
		
	}
	effect = {
		if = {
			limit = {
				has_country_flag = EDG_Caliph_GUI_off_flag
			}
			clr_country_flag = EDG_Caliph_GUI_off_flag
		}
		else = {
			set_country_flag = EDG_Caliph_GUI_off_flag
		}
	}
}
custom_button = {#已打开
	name = Caliph_GUI_on_button
	tooltip = Caliph_GUI_on_button_tt
	potential = {
		NOT = { has_country_flag = EDG_Caliph_GUI_off_flag }
		religion_group = muslim
	}
	trigger = {
		
	}
	effect = {
		if = {
			limit = {
				has_country_flag = EDG_Caliph_GUI_off_flag
			}
			clr_country_flag = EDG_Caliph_GUI_off_flag
		}
		else = {
			set_country_flag = EDG_Caliph_GUI_off_flag
		}
	}
}
custom_icon = {
	name = EDG_Caliph_GUI_bg
	potential = {
		EDG_Caliph_GUI_trigger = yes
	}
	tooltip = "" 
}
