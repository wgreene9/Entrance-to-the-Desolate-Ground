EDG_FKN_disasters_1 = {#法兰克尼亚继承危机
	ended_by_country_breaking_to_rebels = no
	potential = {
		NOT = { has_country_flag = had_EDG_FKN_disasters_1_flag }
		tag = FKN
		is_free_or_tributary_trigger = yes
		uses_doom = no
		normal_or_historical_nations = yes
		is_revolutionary = no
	}
	
	
	can_start = {
		always = yes
	}
	
	can_stop = {
		always = no
	}
	
	
	progress = {
		modifier = {
			factor = 6
			tag = FKN
		}
		modifier = {
			factor = 0.5
			NOT = { stability = 3 }
		}
		modifier = {
			factor = 0.5
			NOT = { stability = 2 }
		}
		modifier = {
			factor = 0.5
			NOT = { stability = 1 }
		}
		modifier = {
			factor = 0.5
			NOT = { stability = 0 }
		}
	}
	
	can_end = {
		
	}
	

	modifier = {
	}	
	
	
	on_monthly = {
		events = {
			flavor_lxa.1
			flavor_lxa.2
			flavor_lxa.3
		}
		random_events = { 
			1000 = 0
		}
	}
	
	
	on_start = EDG_flavorFKN.1 #General Estates / Storming of the Bastille
	
}
