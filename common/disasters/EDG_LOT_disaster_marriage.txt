EDG_LOT_disaster_marriage = {#被荆棘束缚的宫廷
	ended_by_country_breaking_to_rebels = no
	potential = {
		ai = no
		NOT = { has_country_flag = EDG_LOT_disaster_marriage_end_flag }
		or = {
			tag = LOT
			was_tag = LOT
		}
		is_free_or_tributary_trigger = yes
		uses_doom = no
		normal_or_historical_nations = yes
		is_revolutionary = no
	}
	
	
	can_start = {
		has_any_disaster = no
		has_reform = EDG_LOT_feudal_system_disaster
		has_country_flag = EDG_flavor_LOT_flavor_38_a_flag
	}
	
	can_stop = {
		always = no
	}
	
	
	progress = {
		modifier = {
			factor = 10
			always = yes
		}
	}
	
	can_end = {
		OR = {
			custom_trigger_tooltip = {
				tooltip = disaster_active_for_20_years_tooltip
				had_country_flag = {
					flag = EDG_LOT_disaster_marriage_flag
					days = 7300
				}
			}
			custom_trigger_tooltip = {
				tooltip = EDG_LOT_disaster_marriage_can_end_flag_trigger_tooltip
				has_country_flag = EDG_LOT_disaster_marriage_can_end_flag
			}
		}
	}
	

	modifier = {
		stability_cost_modifier = 0.25
	}	
	
	
	on_monthly = {
		events = {
			
		}
		random_events = { 
			1000 = 0
			100 = EDG_LOT_disaster_marriage.14
			100 = EDG_LOT_disaster_marriage.15
		}
	}
	
	
	on_start = EDG_LOT_disaster_marriage.1 #General Estates / Storming of the Bastille
	on_end = EDG_LOT_disaster_marriage.2 #Long live the Republic/Monarchy
	
}
