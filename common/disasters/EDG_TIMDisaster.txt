EDG_TIM_shah_rukh_succession_crisis = {
	potential = {
		tag = TIM
		NOT = {
			has_country_flag = EDG_TIM_disaster_sucession_crisis_end_flag
		}
	}

	can_start = {
		tag = TIM
	}
	
	can_stop = { 
		has_country_flag = EDG_TIM_disaster_sucession_crisis_end_flag
	}
	
	progress = {
		modifier = {
			factor = 20
			tag = TIM
		}
	}
	
	can_end = {
		has_country_flag = EDG_TIM_disaster_sucession_crisis_end_flag
	}

	on_start = EDG_TIM_events.1

	modifier = {
		stability_cost_modifier = 0.5
	}
}

EDG_TIM_calamity_of_astrology = {
	potential = {
		OR = {
			tag = TIM
			was_tag = TIM
		}
		has_country_flag = EDG_TIM_disaster_astrology_start_flag
		NOT = {
			has_country_flag = EDG_TIM_disaster_astrology_end_flag
		}
	}
	
	can_start = {
		has_country_flag = EDG_TIM_disaster_astrology_start_flag
	}
	
	can_stop = {
		always = no
	}
	
	
	progress = {
		modifier = {
			factor = 100
			has_country_flag = EDG_TIM_disaster_astrology_heir_killed_flag
		}
		modifier = {
			factor = 10
			OR = {
				has_country_flag = EDG_TIM_disaster_astrology_heir_drifted_apart_flag
				has_country_flag = EDG_TIM_disaster_astrology_rumor_flag
			}
		}
	}
	
	can_end = {
		has_country_flag = EDG_TIM_disaster_astrology_stable_flag
		NOT = {
			num_of_rebel_armies = 1
		}
		NOT = {
			num_of_rebel_controlled_provinces = 1
		}
	}
	
	modifier = {
		stability_cost_modifier = 1
		advisor_pool = -10
		legitimacy = -2
	}
	
	on_start = EDG_TIM_events.203
	on_end = EDG_TIM_events.224
	
	on_monthly = {
		random_events = {
			1000 = 0
			75 = EDG_TIM_events.211
			75 = EDG_TIM_events.212
			75 = EDG_TIM_events.213
			50 = EDG_TIM_events.214
			50 = EDG_TIM_events.215
			100 = EDG_TIM_events.216
			100 = EDG_TIM_events.217
			50 = EDG_TIM_events.218
			50 = EDG_TIM_events.219
		}
	}
}