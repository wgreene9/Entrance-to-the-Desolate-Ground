estate_ERS_Improving_relations_with_neighboring_countries = {#与邻国改善关系
    can_select = {
		every_country = {
            is_neighbor_of = ROOT
            has_opinion = {
                who = ROOT
                value = -50
            }
        }
	}
    selection_weight = {
		factor = 10
	}
	pre_effect = {
		random_subject_country = {
			limit = {
				is_subject_of_type = rig_knightage_vissal
			}
			save_event_target_as = agenda_country
		}
		capital_scope = {
			save_event_target_as = agenda_province
		}
	}
	provinces_to_highlight = {
		is_capital_of = event_target:agenda_country
	}
	task_requirements = {
		event_target:agenda_country = {
			has_opinion_modifier = {
				who = root
				modifier = sent_gift
			}
		}
	}
    task_completed_effect = {
		add_estate_loyalty = {
			estate = estate_rig_knightage
			loyalty = 20
		}
    }
    failing_effect = {
        add_estate_loyalty_modifier = {
            estate = estate_rig_knightage
			desc = EST_VAL_AGENDA_DENIED
			loyalty = -5
			duration = 7300
        }
    }
}