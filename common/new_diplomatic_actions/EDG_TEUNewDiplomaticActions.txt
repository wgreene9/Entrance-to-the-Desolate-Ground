EDG_call_eastern_garrision = {
	category = emperor
	require_acceptance = no

	is_visible = {
		is_emperor = yes
		is_subject = no
		is_part_of_hre = yes
		from = {
			is_subject = no
			is_part_of_hre = yes
			has_ruler_modifier = EDG_TEU_eastern_garrison_modifier
		}
	}

	is_allowed = {
		NOT = {
			alliance_with = from
		}
		from = {
			is_at_war = no
			NOT = {
				war_with = root
			}
		}
		is_in_war = {
			defender_leader = root
		}
	}

	on_accept = {
		from = {
			join_all_defensive_wars_of = root
		}
	}

	ai_will_do = {
		always = yes
	}
}