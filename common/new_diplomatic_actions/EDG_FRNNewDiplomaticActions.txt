EDG_FRN_exercise_free_city_privilege = {
	category = influence
	require_acceptance = no

	is_visible = {
		has_reform = EDG_FRN_free_city
		from = {
			is_part_of_hre = yes
		}
	}

	is_allowed = {
		dip_power_cost = 50
		NOT = {
			is_rival = from
		}
	}

	on_accept = {
		dip_power_cost = 50
		from = {
			if = {
				limit = { 
					is_rival = root
				}
				remove_rival = root
			}
			set_ai_attitude = {
				attitude = attitude_friendly
				locked = yes
				who = root
			}
			if = {
				limit = { 
					is_claim = 1876
				}
				remove_claim = 1876
			}
		}
	}
	ai_will_do = {
		always = no
	}
}